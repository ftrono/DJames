<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">
    <item
        android:id="@+id/action_send_logs"
        android:title="Send requests logs"
        android:icon="@drawable/send_icon"
        app:showAsAction="never" />
    <item
        android:id="@+id/action_clean_successful"
        android:title="Delete successful logs"
        android:icon="@drawable/item_successful"
        app:showAsAction="never" />
    <item
        android:id="@+id/action_clean"
        android:title="Delete all history logs"
        android:icon="@drawable/delete_icon"
        app:showAsAction="never" />
</menu>