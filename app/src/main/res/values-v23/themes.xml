<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="Theme.DJames" parent="android:Theme.Material.Light.NoActionBar">
        <!-- Transparent system bars for edge-to-edge. -->
        <item name="android:windowBackground">@color/windowBackground</item>
        <item name="android:colorBackground">@color/windowBackground</item>
    </style>

    <style name="Theme.DJames.SplashScreenTheme" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/windowBackground</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/app_icon_round</item>
        <item name="postSplashScreenTheme">@style/Theme.DJames</item>
    </style>

</resources>